baseURL = 'https://tech.isiwari.com/'
languageCode = 'ja-ja'
title = '睦朗'
paginate = '5'
theme = 'PaperMod'
mainSections = ["posts"]

# GitHub Pages で公開するために必要
publishDir = "docs"
canonifyurls = true

googleAnalytics = "G-GSZSPTKC12"

[params]
  # You can use Markdown syntax for urls [text](//url.to/source)
  # To create multi-line text in strings use the """example Text""" format
  #
  # All icons using Fontawesome's icon font. Look at fontawesome.io/icons
  # for more icons. The icons are represented by their corresponding
  # CSS class.

  # Provide your metadata here.
  name = "isiwari"
  # description = "global scale open source software for planning urban futures for active transport"

  # Link custom assets relative to /static
  custom_css = []
  custom_js  = []
  favicon    = "favicon.ico"

# toml config document page is 
# https://gohugo.io/getting-started/configuration/
